import {ColorSchemeType} from 'native-base/lib/typescript/components/types';
import {ColorSchemeName} from 'react-native';

export const boardHeight = 140;
export enum InfoType {
  Recommend = 'recommend',
  Chatter = 'chatter',
}

export const ColorDict: ColorSchemeType[] = [
  'rose',
  'orange',
  'cyan',
  'lime',
  'lightBlue',
];
